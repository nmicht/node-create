{
  "version": "0.0.0-development",
  "name": "${name}",
  "description": "${description}",
  "author": "${author} <${email}> (${website})",
  "homepage": "https://github.com/${github}/${name}",
  "repository": {
    "type": "git",
    "url": "https://github.com/${github}/${name}.git"
  },
  "license": "ISC",
  "main": "build/node4/index.js",
  "keywords": [
    "${name}"
  ],
  "engines": {
    "node": ">=4"
  },
  "files": [
    "build",
    "src"
  ],
  "bugs": {
    "url": "https://github.com/${github}/${name}/issues"
  },
  "scripts": {
    "babel": "babel -q src",
    "build": "for i in node4 node6 node7 browsers; do BABEL_ENV=$i npm run babel -- -d build/$i; done",
    "pretest": "snazzy && echint",
    "test": "tap test --reporter spec --node-arg=--require --node-arg=babel-register",
    "coverage": "tap test --reporter silent --coverage --nyc-arg=--require --nyc-arg=babel-register"
  },
  "standard": {
    "ignore": [
      "build/**"
    ]
  },
  "echint": {
    "ignore": [
      "build/**"
    ]
  },
  "devDependencies": {
    "babel-cli": "*",
    "babel-plugin-add-module-exports": "*",
    "babel-preset-env": "*",
    "babel-register": "*",
    "echint": "*",
    "snazzy": "*",
    "tap": "*"
  }
}
